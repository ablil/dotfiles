--verbose
-H "Content-Type: application/json"
# Save response headers to a file
--dump-header /tmp/curl_headers.txt

# Follow redirects automatically
--location

# Show progress bar
--progress-bar

# Fail on HTTP errors (exit with non-zero status)
--fail

# Set a timeout (seconds)
--max-time 60

# Show errors
--show-error

# Use colored output if supported
--no-clobber
