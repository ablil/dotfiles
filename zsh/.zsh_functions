#!/bin/sh
# Custom bash functions

function mk() {
    mkdir $1 && cd $1
}

function mkgit() {
    mkdir $1 && cd $1 && git init && touch .gitignore
}

function git_repository() {
  gitrepo=$(git branch --show-current 2>/dev/null)
  echo $gitrepo
}
