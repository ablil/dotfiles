# command: gh alias import --clobber <filename>

# create draft pr and self-assign
draft: pr create -a @me --fill-verbose

# create and self-assign issue
todo: issue create -a @me

# merge pull request and delete branch
merge: pr merge -d
smerge: pr merge -d -s
